1. Open VS Code
2. Open the project in VS Code
3. open app.py and set the absolute path in line 49 ad 52 to background image and source file
4. Open the terminal or cmd in the path of the project
5. Ensure Python is Installed (Recommended Version Python 3.10)
6. Install the requiremnts (requirements are listed in the requirements.txt) with the following command
   pip install -r requirements.txt
   Run the app with the following command
   streamlit run src/app.py